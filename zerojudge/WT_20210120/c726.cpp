#include <bits/stdc++.h>
using namespace std;

int main(){
    ios::sync_with_stdio(false);
    cin.tie(0);
    string g, b;
    cin >> g >> b;
    cout << g << " and ";
    cout << b << " sitting in the tree\n";
    return 0;
}