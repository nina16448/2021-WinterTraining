#include <bits/stdc++.h>
using namespace std;
typedef long long lint;

int main(){
    ios::sync_with_stdio(false);
    cin.tie(0);
    lint a, b;
    cin >> a >> b;
    cout << abs(a-b) << "\n";
    return 0;
}