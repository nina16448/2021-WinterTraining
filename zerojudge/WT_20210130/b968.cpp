#include <bits/stdc++.h>
using namespace std;

int main(){
    ios::sync_with_stdio(false);
    cin.tie(0);
    string g, b;

    getline(cin, g);
    getline(cin, b);

    cout << g << " and " << b << " sitting in the tree\n";
    
    return 0;
}